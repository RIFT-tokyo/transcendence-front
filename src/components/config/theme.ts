import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#448FA3'
    },
    secondary: {
      main: '#9A031E'
    },
    background: {
      default: '#ebebd3',
      paper: '#fcf7f8',
    },
  },
})

export default theme
